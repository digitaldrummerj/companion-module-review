# Review — companion-module-biamp-qtx v0.1.2

| | |
|---|---|
| **Module** | biamp-qtx |
| **Review tag** | v0.1.2 |
| **Previous tag** | (none — first release) |
| **Scope** | tag |
| **Language / API** | TypeScript · @companion-module/base `~1.14.1` (v1.x) |
| **Protocol** | HTTP/HTTPS REST (`/api/v1/Config…`, stateless request/response) |
| **Build / lint** | ✅ `yarn build` compiles clean |

> **Note:** `tag` scope was requested, but v0.1.2 is the **first release** (no previous tag → no diff). Per the review process this falls back to a **full review of the whole `src/`**; every finding is therefore NEW.

## Verdict

❌ Changes Required

## 📋 Issues

### Blocking

#### 🟠 High

Please update the license file to match the template file.
